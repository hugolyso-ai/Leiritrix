{
  "name": "leiritrix-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install": "cd frontend && npm install"
  }
}
